 
💻 Angular: basi per creare liste di liste e ulteriori filtri 💻

Come mi hanno consigliato delle persone che hanno cominciato ad utilizzare l'app,
sarebbe consigliato inserire liste di liste, così mi sono messo al lavoro.

Attualmente ho solo in mano una base, tuttavia ho creato un ID con la lista attuale e
poi una base della futura lista di liste:

// Creiamo la base delle nostre note.
interface Todo {
  id: number;
  note: string;  
}

//Creiamo la base della lista di liste
interface NoteList 
{
  list_id: number;
  name: string;
  noteData: Todo[];
}

Poi si crea una variabile che sfrutti NoteList come se fosse un tipo, si crea un template con dei dati di prova
(nel mio caso ho simulato una lista di note principale e un cestino) e poi si devono cambiare delle istruzioni nel
codice.

Non ci sarà più un semplice for che itera una lista di stringhe e l'inserimento, oltre alla loro cancellazione
e modifica di note sarà più verboso come si può comprendere dall'esempio in basso:

//vecchio e attuale metodo per inserire i dati
this.todos.push(newTodo);
//futuro metodo per inserire i dati
this.noteLists[this.current_list].noteData.push(newTodo);

Devo inoltre inserire dei modali, che mi saranno utili sia per confermare la cancellazione di una nota e sia
per scegliere una lista di liste senza problemi. Inoltre ho creato un ulteriore filtro che ordina le note in 
ordine alfabetico (anche essi necessitano di una lista per evitare di creare molteplici bottoni con un solo filtro).

Inoltre, mi sto preparando per caricare il progetto su Github, quindi in futuro vi linkerò la repo.
Vi auguro una buona settimana lavorativa e un buon lavoro, statemi bene. Anche questa settimana passerà velocemente 

#Innovation #Management #DigitalMarketing #Technology #Creativity #Future #Javascript 👋
 
